for a=1:999
    for b=1:999
        if floor((a*b)/100000)==floor((a*b)-((a*b)-floor((a*b)/100000)))
            x=a*b
            if floor((a*b)/10000)-(10*floor((a*b)/100000))==floor((floor((a*b)/1000)-(10000*floor((a*b)/100000)))/1000)
                y=x
                if 
            end
        end
    end
end
            
 y           
            
%num2scr function make the backwards and forwards equal each other
%or use div to chop off numbers